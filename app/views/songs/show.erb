<!% if flash.has?(:message) %>
  <!%= flash[:message] %>
<!% end %>

<h1>Song: <%= @song.name %></h1>

<table style="width:70%">
  <tr>
    <th>Artist</th>
    <th>Genres</th>
  </tr>

  <tr>
    <td> <a href="/artists/<%= @artist.slug %>" ><%= @artist.name %></a> </td>
    
      <td><% @genres.each do |genre| %> <a href="/genres/<%= genre.slug %>"> <%= genre.name %> </a> &nbsp
    <% end %></td>
  </tr>
  
</table>